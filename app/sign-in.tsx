import { useState } from 'react';import { Alert,StyleSheet,Text } from 'react-native';import { router } from 'expo-router';import { Screen } from '@/components/Screen';import { Heading } from '@/components/Heading';import { Card } from '@/components/Card';import { TextField } from '@/components/TextField';import { Button } from '@/components/Button';import { supabase,supabaseConfigured } from '@/services/supabase';import { colors } from '@/theme/theme';
export default function SignIn(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const login=async()=>{if(!supabaseConfigured){Alert.alert('Supabase noch nicht konfiguriert','Trage URL und öffentlichen Anon Key in .env ein.');return;}setBusy(true);const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});setBusy(false);if(error){Alert.alert('Anmeldung fehlgeschlagen',error.message);return;}router.replace('/onboarding');};return <Screen><Heading title="Anmelden" subtitle="Supabase Auth – Sitzungsdaten werden über SecureStore gespeichert."/><Card><TextField label="E-Mail" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail}/><TextField label="Passwort" secureTextEntry value={password} onChangeText={setPassword}/><Button label={busy?'Anmeldung…':'Anmelden'} onPress={login} disabled={busy}/><Button label="Zurück" variant="secondary" onPress={()=>router.back()}/></Card><Text style={styles.note}>Für Cloud-Synchronisierung muss der angemeldete Benutzer Mitglied des jeweiligen Haushalts in Supabase sein.</Text></Screen>}
const styles=StyleSheet.create({note:{color:colors.textMuted,fontSize:13,lineHeight:18}});
