name: HomeOS Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Manual placeholder
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic builds are temporarily paused during the HomeOS UI refactor."
